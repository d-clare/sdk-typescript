{
  "name": "@d-clare/sdk",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "prepare": "git submodule update --remote --merge",
    "build": "tsc && vite build",
    "test": "vitest run",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.1.2",
    "json-schema-typed": "^8.0.1",
    "typescript": "~5.7.2",
    "vite": "^6.3.1",
    "vitest": "^3.1.2"
  },
  "dependencies": {
    "class-transformer": "^0.5.1",
    "reflect-metadata": "^0.2.2",
    "yaml": "^2.7.1"
  }
}
